# dot-files
so these are my dot files uhm yeah feel free to use them idc im gnna update this quite often so ye
# Installation 
you will get the following configs

Fastfetch — Fastfetch configuration
Fish — Fish shell configuration
Kitty — Kitty terminal configuration
Tofi — Tofi configuration
Installation

Clone the repository:

git clone https://github.com/krinkyroot/dot-files.git

Enter the directory:

cd dot-files

Run the installer:

bash install.sh

and ur done i ratted u haha loser
